import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Avatar, darkAvatarTheme } from '../../../src/elements/Avatar';
import * as AvatarStories from '../../../src/elements/Avatar/Avatar.story'
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Elements/Avatar" />

# Avatar
An avatar component commonly used to provide a visual identity for users. This is mostly used for showing user profile pictures,
team members profile pics for a team details page.

## Example
### Avatar showing initials from the user name
If the user name is single word text, it will just show the first letter
<Canvas sourceState="shown" of={AvatarStories.Default}/>

### Avatar with round shape showing user profile pic
<Canvas of={AvatarStories.RoundedWithImage}/>

## Theme
This component uses the following default theme:

<ThemeRender theme={darkAvatarTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Avatar} />
