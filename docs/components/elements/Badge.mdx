import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Badge, darkBadgeTheme } from '../../../src/elements/Badge';
import * as BadgeStories from '../../../src/elements/Badge/Badge.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Elements/Badge" />

# Badge
Simple badge component, typically used to show number on top of an existing component to grab user's attention. Example, new msgs count,
new alert counts etc.

## Examples
### Badge with color variations
<Canvas sourceState="shown" of={BadgeStories.Colors} />
### Badge position variations
<Canvas of={BadgeStories.Positions} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkBadgeTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Badge} />
