import { Meta, Canvas } from '@storybook/addon-docs';
import { Kbd } from '../../../src/elements/Kbd';
import * as Stories from '../../../src/elements/Kbd/Kbd.story'

<Meta title="Docs/Components/Elements/Kbd" />

# Kbd
Keyboard shortcut chip. These can be used to show on hover shorcuts mapped to UI components and can be used along with
the [Command Palette](?path=/docs/docs-components-elements-command-palette--docs) component as well.

## Example
<Canvas sourceState="shown" of={Stories.Basic}/>

## Design Tokens
This component uses the following design tokens:

- `border-radius-sm`
- `mono-font-family`

Learn more about design tokens in the [design tokens documentation](?path=/story/docs-design-tokens-getting-started--page).

## API

[//]: # (<ArgsTable of={Kbd} />)
