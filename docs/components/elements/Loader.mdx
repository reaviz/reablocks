import { Meta, Canvas } from '@storybook/addon-docs';
import { DotsLoader } from '../../../src/elements/Loader';
import * as DotsLoaderStories from '../../../src/elements/Loader/DotsLoader.story';

<Meta title="Docs/Components/Elements/Loader" />

# Loader
A placholder component to show when the UI is being rendered for firt time load or when data is being fetched.

## Example
<Canvas sourceState="shown" of={DotsLoaderStories.Simple} />

## Design Tokens
This component uses the following design tokens:

- `loader-background`

Learn more about design tokens in the [design tokens documentation](?path=/story/docs-design-tokens-getting-started--page).

## API

[//]: # (<ArgsTable of={DotsLoader} />)
