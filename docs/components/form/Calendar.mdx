import { Meta, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Calendar } from '../../../src/form/Calendar';
import * as CalendarStories from '../../../src/form/Calendar/Calendar.story';

<Meta title="Docs/Components/Form/Calendar" />

# Calendar

## Examples
<Canvas sourceState="shown" of={CalendarStories.Simple} />
<Canvas sourceState="shown" of={CalendarStories.Disabled} />
<Canvas sourceState="shown" of={CalendarStories.NoAnimation} />
<Canvas sourceState="shown" of={CalendarStories.Range} />
<Canvas sourceState="shown" of={CalendarStories.MinMax} />
<Canvas sourceState="shown" of={CalendarStories.DefaultValue} />

## Design Tokens
This component uses the following design tokens:

- `calendar-spacing`

Learn more about design tokens in the [design tokens documentation](?path=/story/docs-design-tokens-getting-started--page).

## API
<ArgsTable of={Calendar} />
