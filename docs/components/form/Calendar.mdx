import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Calendar, darkCalendarTheme } from '../../../src/form/Calendar';
import * as CalendarStories from '../../../src/form/Calendar/Calendar.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Form/Calendar" />

# Calendar
Calendar component is generally used in combination with other UI components like search box etc. to allow the users to
define the time range for their action.

## Examples
### Basic example with single date selection
<Canvas sourceState="shown" of={CalendarStories.Simple} />
### Date range selection
<Canvas of={CalendarStories.Range} />
### Restricted date range
<Canvas of={CalendarStories.MinMax} />
### Initialise with default date (E.g.: one month from now)
<Canvas of={CalendarStories.DefaultValue} />
### Example with day of week labels
<Canvas of={CalendarStories.WithLabels} />
### Allow range selection
<Canvas of={CalendarStories.Range} />
### Multiview calendar example
<Canvas of={CalendarStories.Multiview} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkCalendarTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Calendar} />
