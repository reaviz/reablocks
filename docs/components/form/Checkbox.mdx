import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Checkbox, darkCheckboxTheme } from '../../../src/form/Checkbox';
import * as CheckboxStories from '../../../src/form/Checkbox/Checkbox.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Form/Checkbox" />

# Checkbox
Checkbox components in UI/UX serve to enable or disable specific options or choices, enhancing user control and interaction with selectable items.

## Examples
### Basic example
<Canvas sourceState="shown" of={CheckboxStories.Simple} />
### Checkboxes with size variations
<Canvas of={CheckboxStories.Sizes} />
### Checkboxes with state variations
<Canvas of={CheckboxStories.States} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkCheckboxTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Checkbox} />
