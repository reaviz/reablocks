import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkRadioTheme, Radio } from '../../../src/form/Radio';
import { RadioGroup } from '../../../src/form/Radio';
import * as RadioStories from '../../../src/form/Radio/Radio.story';
import * as RadioGroupStories from '../../../src/form/Radio/RadioGroup.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Form/Radio" />

# Radio

Radio elements in UI/UX are used for exclusive selection among a set of options,
ensuring users choose a single item from a list, providing clear decision-making and control.

## Examples
### Basic example
<Canvas sourceState="shown" of={RadioStories.Simple} />
### Radio element with size variations
<Canvas of={RadioStories.Sizes} />
### Disabled state
<Canvas of={RadioStories.Disabled} />
### Group of Radio buttons with a single form control
<Canvas of={RadioGroupStories.Simple} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkRadioTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
### Radio
<ArgTypes of={Radio} />

### RadioGroup
<ArgTypes of={RadioGroup} />
