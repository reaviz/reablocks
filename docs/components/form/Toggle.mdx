import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkToggleTheme, Toggle } from '../../../src/form/Toggle';
import * as ToggleStories from '../../../src/form/Toggle/Toggle.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Form/Toggle" />

# Toggle
The Toggle component provides a user-friendly way to switch between two states, such as on/off or enabled/disabled, enhancing interactivity in user interfaces.
It's commonly used for settings, preferences, and quick mode changes.

## Examples
### Basic example
<Canvas sourceState="shown" of={ToggleStories.Simple} />
### Toggle size variations
<Canvas sourceState="shown" of={ToggleStories.Sizes} />
### Toggle state: disabled
<Canvas sourceState="shown" of={ToggleStories.Disabled} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkToggleTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Toggle} />
