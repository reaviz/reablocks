import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkDialogTheme, Dialog } from '../../../src/layers/Dialog';
import * as DialogStories from '../../../src/layers/Dialog/Dialog.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layers/Dialog" />

# Dialog
The Dialog component serves as a pop-up or modal window that overlays the main content,
allowing the display of important messages, forms, or user interactions while grabbing user's focus.

## Example
### Basic Dialog
<Canvas sourceState="shown" of={DialogStories.Simple} />
### Dialog with Footer and Custom header
<Canvas of={DialogStories.Footer} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkDialogTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Dialog} />
