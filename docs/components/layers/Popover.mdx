import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { popoverTheme, Popover } from '../../../src/layers/Popover';
import * as PopoverStories from '../../../src/layers/Popover/Popover.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layers/Popover" />

# Popover
The Popover component displays addtional contextual information, content, or options when user perform an action on the UI element tied to it.

## Example
<Canvas sourceState="shown" of={PopoverStories.Simple} />

## Theme
This component uses the following default theme:

<ThemeRender theme={popoverTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).


## API
<ArgTypes of={Popover} />
