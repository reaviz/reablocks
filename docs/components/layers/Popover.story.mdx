import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Popover } from '../../../src/layers/Popover';

<Meta title="Docs/Components/Layers/Popover" />

# Popover

## CSS Variables
The popover component uses the following CSS variables.
If you are using the `DesignTokenProvider` these will be defined
in the `component` section.

- `--popover-background`
- `--popover-color`
- `--popover-border-radius`

## Example
<Canvas>
  <Story id="components-layers-popover--simple" />
</Canvas>

## API
<ArgsTable of={Popover} />
