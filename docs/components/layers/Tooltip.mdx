import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { tooltipTheme, Tooltip } from '../../../src/layers/Tooltip';
import * as TooltipStories from '../../../src/layers/Tooltip/Tooltip.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layers/Tooltip" />

# Tooltip
A Tooltip is a small, informative pop-up that provides context, explanations, or additional details when a user hovers over or interacts with specific elements in a user interface.

## Example
### Basic
<Canvas sourceState="shown" of={TooltipStories.Simple} />
### Disabled
<Canvas of={TooltipStories.Disabled} />

## Theme
This component uses the following default theme:

<ThemeRender theme={tooltipTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Tooltip} />
