import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Tooltip } from '../../../src/layers/Tooltip';

<Meta title="Docs/Components/Layers/Tooltip" />

# Tooltip

## CSS Variables
The tooltip component uses the following CSS variables.
If you are using the `DesignTokenProvider` these will be defined
in the `component` section.

- `--tooltip-background`
- `--tooltip-color`
- `--tooltip-border-radius`

## Example
<Canvas>
  <Story id="components-layers-tooltip--simple" />
</Canvas>

## API
<ArgsTable of={Tooltip} />
