import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Block, blockTheme } from '../../../src/layout/Block';
import * as BlockStories from '../../../src/layout/Block/Block.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Block" />

# Block
Simple block component.

## Example
<Canvas sourceState="shown" of={BlockStories.Label} />

## Theme
This component uses the following default theme:

<ThemeRender theme={blockTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Block} />
