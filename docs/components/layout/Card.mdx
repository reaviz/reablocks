import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Card, darkCardTheme } from '../../../src/layout/Card';
import * as CardStories from '../../../src/layout/Card/Card.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Card" />

# Card
Cards are a fundamental building block for compositions, such as forms, or sections.

## Example
<Canvas sourceState="shown" of={CardStories.Basic}/>

## Theme
This component uses the following default theme:

<ThemeRender theme={darkCardTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Card} />
