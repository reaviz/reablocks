import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkDividerTheme, Divider } from '../../../src/layout/Divider';
import * as DividerStories from '../../../src/layout/Divider/Divider.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Divider" />

# Divider
A simple divider component that supports horizontal and vertical.

## Example
<Canvas sourceState="shown" of={DividerStories.Horizontal} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkDividerTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Divider} />
