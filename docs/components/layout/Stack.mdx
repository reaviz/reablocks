import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkStackTheme, Stack } from '../../../src/layout/Stack';
import * as StackStories from '../../../src/layout/Stack/Stack.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Stack" />

# Stack
Component that sets component spacing. It can be used to add
space between components such as a menu.

## Example
<Canvas sourceState="shown" of={StackStories.Simple} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkStackTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={Stack} />
