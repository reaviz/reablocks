import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { Tabs, Tab, TabList, TabPanel, tabsTheme } from '../../../src/layout/Tabs';
import * as TabsStories from '../../../src/layout/Tabs/Tabs.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Tabs" />

# Tabs
The tabs component is a simple way to organize content into different sections.

## Example
<Canvas sourceState="shown" of={TabsStories.Simple} />

## Theme
This component uses the following default theme:

<ThemeRender theme={tabsTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API

### Tabs
<ArgTypes of={Tabs} />

### TabList
<ArgTypes of={TabList} />

### Tab
<ArgTypes of={Tab} />

### TabPanel
<ArgTypes of={TabPanel} />
