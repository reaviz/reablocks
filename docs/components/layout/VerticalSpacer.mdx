import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs';
import { darkVerticalSpacerTheme, VerticalSpacer } from '../../../src/layout/VerticalSpacer';
import * as VerticalSpacerStories from '../../../src/layout/VerticalSpacer/VerticalSpacer.story';
import ThemeRender from '../../ThemeRender.tsx';

<Meta title="Docs/Components/Layout/Vertical Spacer" />

# Vertical Spacer
Simple component to add vertical spacing between elements.

## Example
<Canvas sourceState="shown" of={VerticalSpacerStories.Simple} />

## Theme
This component uses the following default theme:

<ThemeRender theme={darkVerticalSpacerTheme} />

Learn more about how to customize in the [Theme documentation](?path=/docs/docs-theme-getting-started--docs).

## API
<ArgTypes of={VerticalSpacer} />
